# Pasta para certificados SSL (não commitar arquivos .pem/.key/.crt)
# Em desenvolvimento, usar certificado autoassinado:
#   openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#     -keyout key.pem -out cert.pem -subj "/CN=localhost"
