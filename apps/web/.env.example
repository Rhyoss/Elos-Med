# Copia este arquivo para .env.local e ajuste os valores

# Habilita sessão mock de dev (owner com todas as permissões) quando não há backend rodando.
# Nunca use true em produção.
NEXT_PUBLIC_DEV_MOCK_AUTH=true
